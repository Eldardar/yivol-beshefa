import { AppShell } from "@/components/nav";import { db,requireUser } from "@/lib/server";import { formatHebrewDate,jerusalemDate } from "@/lib/dates";import { UNIT_LABEL, type Unit } from "@/lib/units";
export const dynamic="force-dynamic";export default async function History(){const user=await requireUser();if(user.role!=="PICKER")return null;const rows=db().prepare(`SELECT s.id,s.date,s.slot,s.status,f.name farm,se.crop FROM shift_pickers sp JOIN shifts s ON s.id=sp.shift_id JOIN seasons se ON se.id=s.season_id JOIN farms f ON f.id=se.farm_id WHERE sp.user_id=? AND s.status IN ('PUBLISHED','COMPLETED','CANCELLED') AND (s.date<? OR s.status IN ('COMPLETED','CANCELLED')) ORDER BY s.date DESC`).all(user.id,jerusalemDate()) as Array<{id:number;date:string;slot:string;status:string;farm:string;crop:string}>;const quantityRows=db().prepare("SELECT shift_id,quantity,unit FROM quantities WHERE user_id=?").all(user.id) as Array<{shift_id:number;quantity:number;unit:Unit}>;const quantitiesByShift=new Map<number,Array<{quantity:number;unit:Unit}>>();for(const q of quantityRows){const arr=quantitiesByShift.get(q.shift_id)??[];arr.push({quantity:q.quantity,unit:q.unit});quantitiesByShift.set(q.shift_id,arr);}return <AppShell user={user}><h1>היסטוריה וכמויות</h1><div className="table-wrap card"><table className="table"><thead><tr><th>תאריך</th><th>משמרת</th><th>חקלאי</th><th>גידול</th><th>מצב</th><th>כמות</th></tr></thead><tbody>{rows.map(x=>{const lines=quantitiesByShift.get(x.id)??[];return <tr key={x.id}><td>{formatHebrewDate(x.date)}</td><td>{x.slot==="MORNING"?"בוקר":"ערב"}</td><td>{x.farm}</td><td>{x.crop}</td><td>{({DRAFT:"טיוטה",PUBLISHED:"פורסמה",COMPLETED:"הושלמה",CANCELLED:"בוטלה"} as Record<string,string>)[x.status]??x.status}</td><td>{lines.length?lines.map(l=>`${l.quantity} ${UNIT_LABEL[l.unit]}`).join(" · "):"—"}</td></tr>;})}</tbody></table></div></AppShell>}
