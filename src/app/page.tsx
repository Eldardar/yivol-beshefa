import Link from "next/link";import { AppShell } from "@/components/nav";import { db,requireUser } from "@/lib/server";
export const dynamic="force-dynamic";export default async function Home(){const user=await requireUser();const database=db();if(user.role==="ADMIN"){const stats={pickers:(database.prepare("SELECT count(*) n FROM users WHERE role='PICKER' AND active=1").get() as {n:number}).n,shifts:(database.prepare("SELECT count(*) n FROM shifts WHERE status='PUBLISHED' AND date>=date('now')").get() as {n:number}).n,farms:(database.prepare("SELECT count(*) n FROM farms WHERE active=1").get() as {n:number}).n};return <AppShell user={user}><section className="hero"><h1>שלום {user.name}</h1><p>תמונת מצב תפעולית עדכנית</p></section><div className="grid"><article className="card"><div className="metric">{stats.pickers}</div><h2>קוטפים פעילים</h2></article><article className="card"><div className="metric">{stats.shifts}</div><h2>משמרות קרובות</h2></article><article className="card"><div className="metric">{stats.farms}</div><h2>משקים פעילים</h2></article><Link className="card" href="/admin/shifts"><h2>ניהול משמרות ←</h2><p>יצירה, פרסום ודיווח כמויות</p></Link></div></AppShell>};const next=database.prepare(`SELECT s.id,s.date,s.slot,f.name farm,se.crop FROM shift_pickers sp JOIN shifts s ON s.id=sp.shift_id JOIN seasons se ON se.id=s.season_id JOIN farms f ON f.id=se.farm_id WHERE sp.user_id=? AND s.status='PUBLISHED' AND s.date>=date('now') ORDER BY s.date LIMIT 1`).get(user.id) as {id:number;date:string;slot:string;farm:string;crop:string}|undefined;return <AppShell user={user}><section className="hero"><h1>שלום {user.name}</h1><p>{next?`השיבוץ הבא: ${next.date} · ${next.slot==="MORNING"?"בוקר":"ערב"} · ${next.farm}`:"אין שיבוצים קרובים"}</p></section><div className="grid"><Link className="card" href="/availability"><h2>עדכון זמינות</h2><p>דיווח זמינות לחודש הבא</p></Link><Link className="card" href="/assignments"><h2>השיבוצים שלי</h2><p>פרטי עבודה וניווט</p></Link><Link className="card" href="/history"><h2>היסטוריה וכמויות</h2></Link></div></AppShell>}
