import { AppShell } from "@/components/nav";import { csrfValue,db,requireUser } from "@/lib/server";import { formatHebrewDateTime } from "@/lib/dates";
export const dynamic="force-dynamic";export default async function Notifications(){const user=await requireUser();const csrf=await csrfValue();const rows=db().prepare("SELECT id,title,body,read_at,created_at FROM notifications WHERE user_id=? ORDER BY created_at DESC").all(user.id) as Array<{id:number;title:string;body:string;read_at:string|null;created_at:string}>;return <AppShell user={user}><h1>התראות</h1><div className="stack">{rows.map(x=><article className="card" key={x.id}><h2>{x.title}</h2><p>{x.body}</p><small className="muted">{formatHebrewDateTime(x.created_at)}</small>{!x.read_at&&<form action="/api/actions" method="post"><input type="hidden" name="action" value="readNotification"/><input type="hidden" name="csrf" value={csrf}/><input type="hidden" name="notificationId" value={x.id}/><button className="btn secondary">סימון כנקראה</button></form>}</article>)}{!rows.length&&<p className="card">אין התראות.</p>}</div></AppShell>}
