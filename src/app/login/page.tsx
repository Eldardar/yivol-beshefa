import { currentUser } from "@/lib/server";import { redirect } from "next/navigation";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser())redirect("/");const {error}=await searchParams;return <main className="shell login"><section className="card"><h1>🌾 יבול בשפע</h1><p className="muted">מערכת ניהול תפעול חקלאי</p>{error&&<p className="alert" role="alert">פרטי ההתחברות שגויים</p>}<form action="/api/login" method="post" className="stack"><div className="field"><label htmlFor="email">דוא״ל</label><input className="input" id="email" name="email" type="email" autoComplete="username" required maxLength={254}/></div><div className="field"><label htmlFor="password">סיסמה</label><input className="input" id="password" name="password" type="password" autoComplete="current-password" required maxLength={128}/></div><button className="btn" type="submit">כניסה מאובטחת</button></form></section></main>}
