import Link from "next/link";import { AppShell } from "@/components/nav";import { db,requireAdmin } from "@/lib/server";import { formatHebrewDate,nextJerusalemMonth } from "@/lib/dates";
export const dynamic="force-dynamic";export default async function Admin(){const user=await requireAdmin();const database=db(),month=nextJerusalemMonth();const availability=database.prepare(`SELECT u.name,a.date,a.status FROM availability a JOIN users u ON u.id=a.user_id WHERE a.date>=? AND a.date<? ORDER BY a.date,u.name LIMIT 100`).all(month.start,month.end) as Array<{name:string;date:string;status:string}>;return <AppShell user={user}><section className="hero"><h1>מרכז התפעול</h1><p>זמינות, משמרות ומשאבים במקום אחד</p></section><div className="grid"><Link className="card" href="/admin/shifts"><h2>משמרות</h2><p>יצירה, פרסום, ביטול ודיווח</p></Link><Link className="card" href="/admin/users"><h2>עובדים</h2><p>ניהול חשבונות ופרופילים</p></Link><Link className="card" href="/admin/resources"><h2>חקלאים, עונות ורכבים</h2></Link></div><h2>זמינות בחודש הבא</h2><div className="table-wrap card"><table className="table"><thead><tr><th>תאריך</th><th>קוטף</th><th>מצב</th></tr></thead><tbody>{availability.map((a,i)=><tr key={i}><td>{formatHebrewDate(a.date)}</td><td>{a.name}</td><td>{a.status==="AVAILABLE"?"רוצה לעבוד":a.status==="MAYBE"?"אולי":"מנוחה"}</td></tr>)}</tbody></table>{!availability.length&&<p>טרם דווחה זמינות.</p>}</div></AppShell>}
