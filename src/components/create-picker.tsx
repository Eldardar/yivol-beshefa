"use client";import {useState} from "react";
export function CreatePicker({csrf}:{csrf:string}){const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");setPassword("");const form=new FormData(e.currentTarget);const body=Object.fromEntries(form);try{const res=await fetch("/api/admin/users",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...body,csrf})});const data=await res.json();if(!res.ok)throw new Error(data.error||"הפעולה נכשלה");setPassword(data.initialPassword);e.currentTarget.reset()}catch(e){setError(e instanceof Error?e.message:"הפעולה נכשלה")}finally{setBusy(false)}}return <section className="card"><h2>הוספת משתמש</h2><form className="stack" onSubmit={submit}><div className="field"><label>שם מלא<input className="input" name="name" required maxLength={100}/></label></div><div className="field"><label>דוא״ל<input className="input" name="email" type="email" required maxLength={254}/></label></div><div className="field"><label>טלפון<input className="input" name="phone" required maxLength={30}/></label></div><div className="field"><label>תפקיד<select className="input" name="role" defaultValue="PICKER"><option value="PICKER">קוטף</option><option value="ADMIN">מנהל</option></select></label></div><div className="field"><label>הערות<textarea className="input" name="notes" maxLength={2000}/></label></div><button className="btn" disabled={busy}>{busy?"יוצר…":"יצירה וסיסמה זמנית"}</button></form>{error&&<p className="alert" role="alert">{error}</p>}{password&&<div className="alert" role="status"><strong>הסיסמה מוצגת פעם אחת בלבד:</strong><p><code>{password}</code></p><p>העתיקו ומסרו לקוטף בערוץ מאובטח. היא אינה נשמרת בטקסט גלוי.</p></div>}</section>}
