name: בדיקות
on: [push, pull_request]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm audit --audit-level=low
      - run: DATABASE_PATH=/tmp/yivol-ci.sqlite npm run db:migrate
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: DATABASE_PATH=/tmp/yivol-ci.sqlite npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run e2e
