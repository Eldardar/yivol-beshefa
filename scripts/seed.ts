import { getDb } from "../src/lib/db";import { hashPassword,normalizeEmail } from "../src/lib/security";const {SEED_ADMIN_NAME:name,SEED_ADMIN_EMAIL:email,SEED_ADMIN_PHONE:phone,SEED_ADMIN_PASSWORD:password}=process.env;if(!name||!email||!phone||!password){console.error("חובה להגדיר SEED_ADMIN_NAME, SEED_ADMIN_EMAIL, SEED_ADMIN_PHONE, SEED_ADMIN_PASSWORD");process.exit(1)}if(password.length<12){console.error("הסיסמה חייבת להכיל לפחות 12 תווים");process.exit(1)}const db=getDb();const hash=await hashPassword(password);db.prepare("INSERT INTO users(name,email,phone,role,password_hash,active) VALUES(?,?,?,?,?,1) ON CONFLICT(email) DO NOTHING").run(name,normalizeEmail(email),phone,"ADMIN",hash);console.log("חשבון המנהל נוצר או כבר קיים. הסיסמה אינה מודפסת.");db.close();
